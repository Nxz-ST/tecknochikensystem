server:
  port: ${PORT:8867}
message: Configuracion de registro en el servidor levantado!
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: 123456789
    url: jdbc:mysql://localhost:3306/crudzevaz?serverTimezone=UTC
    username: root
  jpa:
    hibernate:
      ddl-auto: update
      database-platform: org.hibernate.dialect.MySQLDialect
  cloud:
    config:
      uri: http://localhost:8860
eureka:
  client:
    fetch-registry: false
    registre-with-eureka: false
#message: Servicio de registro y descrubimiento levantado en el servidor!
